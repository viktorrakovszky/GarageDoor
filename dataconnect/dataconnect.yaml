specVersion: "v1beta"
serviceId: "garage-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "garage-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
